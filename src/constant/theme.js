import { createTheme } from "@mui/material";

export default createTheme({
    primary: {
        main: '#4CAF50', // Couleur principale (vert)
        dark: '#388E3C', // Couleur de survol (plus foncée)
        contrastText: '#FFFFFF', // Couleur du texte sur le bouton
    },
});